# Gyllenhaal Experiment 

The .html file contains all the executable js code to run the experiment in a browser, and it use the config files available in the js folder. 
The py folder contains a jupyter notebook script to clean the CSV file obtained from jsPsych, to get the response;
The audio and img folder contains elements to display.
